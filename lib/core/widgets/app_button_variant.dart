/// Semantic variants for [AppButton].
///
/// Maps to distinct visual treatments driven by [AppButtonTheme].
enum AppButtonVariant {
  /// Primary CTA — filled background, highest emphasis.
  primary,

  /// Secondary action — outlined or tonal, medium emphasis.
  secondary,

  /// Destructive action — filled with error/danger color.
  danger,
}
